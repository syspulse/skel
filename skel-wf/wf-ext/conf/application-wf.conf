include "default-core"
include "default-http"
include "default-wf"

http {
  routes {
    ask-timeout = 60s
  }
}

# Async postgres
postgres0 {
  dataSourceClassName=org.postgresql.ds.PGSimpleDataSource
  url="jdbc:postgresql://localhost:5432"  
  database=explain_db
  username=explain_user
  password=explain_pass
  dataSource.connectionProperties="TimeZone=UTC;binaryTransfer=false;disableTimeZone=true"
  numThreads = 10  
}

postgres {
  dataSourceClassName=org.postgresql.ds.PGSimpleDataSource
  url=${?DB_URL}
  #host=localhost  
  database=${?DB_DATABASE}
  username=${?DB_USER}
  password=${?DB_PASS}
  dataSource.connectionProperties="TimeZone=UTC;binaryTransfer=false;disableTimeZone=true"
  numThreads = 10  
}

#datastore = "mem://"
# search=fts|tgram|fts,tgram — index kinds (not the postgres JDBC url)
datastore = "jdbc://postgres?search=tgram"

