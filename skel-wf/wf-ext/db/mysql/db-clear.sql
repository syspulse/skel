USE workflow_db;
-- detector / detector_schema are EXTERNAL (another product) and are NOT truncated here.
TRUNCATE TABLE workflow_schema;
TRUNCATE TABLE workflow_config;
TRUNCATE TABLE workflow_graf;
